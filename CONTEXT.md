# CONTEXT

## 当前正在做什么
- 正在把项目整理成明天可远程展示的公开版本。
- 当前主展示入口是 `https://rose-financial-agent-demo-20260420.loca.lt`。
- 代码已固定到演示分支 `codex/demo-2026-04-20`。

## 上次停在哪个位置
- 本地发布前检查已通过：前端构建通过、后端测试通过、健康检查正常。
- 公网地址已验证可打开首页、终端和历史记录接口，并已跑通中文研究、英文研究、历史模式 3 条演示链路。
- Railway 正式部署还没完成，因为当前机器没有现成 Railway 登录态，暂时先用 localtunnel 保证外网可访问。

## 近期关键决定与原因
- 先用 localtunnel 提供公开网址，是为了在不打断用户休息的情况下先拿到“明天能直接演示”的结果。
- 发布采用独立分支而不是直接继续改 `main`，是为了固定演示版本，避免明天前再改动影响展示。
- 演示时优先使用 `/` 和 `/terminal`，需要展示历史能力时再进入 `/terminal/backtest` 和 `/terminal/archive`，这样更稳、更好讲。