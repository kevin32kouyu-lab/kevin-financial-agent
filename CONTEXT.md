# CONTEXT

## 当前正在做什么
- 正在做“已落地功能质量补强”：增强可控多智能体、RAG 引用、结论校验、回测口径和 debug 可观测性。
- 本轮不改公开 API，不重做普通用户前台视觉。

## 上次停在哪个位置
- 已补强 `research_plan`：增加候选工具和失败降级策略。
- 已补强 `agent_trace`：增加状态、开始/结束时间、耗时、证据数量和失败原因。
- 已补强 RAG 证据：增加时效、来源可靠性和使用区域。
- 已补强回测：默认写入交易成本、滑点、分红和再平衡口径。
- 已在 `/debug` 增加 Agent Trace 可视化。
- 当前在隔离 worktree 分支 `codex/quality-hardening` 开发。

## 近期关键决定与原因
- 多智能体仍保持“可控多角色流程”，不做完全自治辩论：更稳定，也更适合演示和论文说明。
- `confidence_level` 由 ValidatorAgent 统一计算：避免多个模块各写各的可信度。
- 回测采用 V1.5 保守口径：先把交易成本、滑点、分红和再平衡说明清楚，避免用户误解收益精度。
- 普通终端继续隐藏系统理解、长期记忆和覆盖说明；这些内部信息只放到 debug / artifact。
- 原主目录仍有原本存在的未跟踪目录 `.npm-cache/`，本轮未处理。
